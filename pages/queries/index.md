---
title: Query Library
---

Reusable SQL lives in the top-level `queries/` folder.

- Add SQL files to `queries/`
- Reference them in page frontmatter

Example:

```yaml
queries:
  - sales: sales.sql
```
